{
  "name": "htpi-admin-portal",
  "version": "1.0.0",
  "description": "HTPI Admin Portal - Socket.IO Client",
  "scripts": {
    "start": "serve -s . -l ${PORT:-3001}",
    "dev": "serve -s . -l 3001"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}